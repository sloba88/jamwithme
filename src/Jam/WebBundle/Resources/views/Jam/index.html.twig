{% extends '::base.html.twig' %}

{% block body %}

    <ul>
        {% for j in jams %}
            <li>
                <a href="{{ path('view_jam', {'slug': j.slug }) }}">{{ j.name  }}</a>
            </li>
        {% endfor %}
    </ul>

{% endblock %}