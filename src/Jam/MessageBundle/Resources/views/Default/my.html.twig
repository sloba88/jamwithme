{% extends '::base.html.twig' %}

{% block body %}

    <p>My Messages</p>
    {% for m in messages %}
        <div class="col-md-6">
            <p>{{ m.from.username }}: {{ m.message }}</p>
        </div>
    {% endfor %}

{% endblock %}